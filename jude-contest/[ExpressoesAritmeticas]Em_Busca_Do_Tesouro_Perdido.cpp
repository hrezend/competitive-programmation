#include <bits/stdc++.h>
using namespace std;

int main(){
    long int answer, L, Z, N, U, S;
    cin >> L >> Z >> N >> U >> S;
    
    answer = (L + Z + N + U + S);
    
    cout << answer << endl;
}
